/* 
Copy over fixtures, write test to confirm it will change a given unit to another given unit.
I am doing this frontend because I don't want to mess with the integrity of the original recipe as it is entered, and to be convertible quickly in the frontend.

Do I want to make the recipes have an option for the conversion? 
*/

export default function ConversionService(amt, unit) {
  
}